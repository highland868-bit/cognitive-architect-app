Temporary placeholders are in place: a plain pulsing-circle Lottie clip per
state (different color per state), just so the app runs without crashing on
a missing asset.

Replace these with the real 4 files from the master plan's "LOTTIE ASSETS"
section, keeping the exact same names:

- character.json   (IDLE, THINKING, SPEAKING)
- breathing.json   (BREATHING)
- reflective.json  (REFLECTIVE, GROUNDING)
- crisis.json      (CRISIS)

Delete this placeholder file once the real ones are in place.
